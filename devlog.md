https://github.com/188738/osProject1Salzaar/settings


Wednesday March 11th evening:

There are 3 classes for this project. One of them is the driver class, which is what the user needs to communicate with. One of them is the Encryption class. One of them is the Logger class. In the driver program we need the methods password, encrypt, decrypt, and history. You do not store password in the history. I started the driver class because I was confused about the whole flow.  


Friday March 11th 12:36 PM:

I fixed the driver class. I understood that there were a few errors with the way I was initializing things. I finished all the methods. Logger only writes to logger.txt. The 3 commands that encrypt handles are passkey, encrypt, and decrypt using the cipher. I need to implement this later in the evening as well as the logger class. 

Friday March 11th 7:04 Pm

I finished everything including the encryption and the logger class. The logger class logs data to the file. The encrypt class has the cipher logic. 

